<UserControl x:Class="JiraHelper.Core.IssueDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:JiraHelper.Core"
             MinHeight="400" MinWidth="400">
    <UserControl.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="250"/>
            <ColumnDefinition Width="*" MinWidth="220"/>
        </Grid.ColumnDefinitions>
        <!-- Issue Details + Comments -->
        <Grid Grid.Column="0" Margin="0,0,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Key:" FontWeight="Bold" Grid.Row="0"/>
            <TextBlock x:Name="KeyText" Margin="120,0,0,0" Grid.Row="0"/>
            <TextBlock Text="Summary:" FontWeight="Bold" Margin="0,10,0,0" Grid.Row="1"/>
            <TextBlock x:Name="SummaryText" Margin="120,0,0,0" Grid.Row="1"/>
            <TextBlock Text="Status:" FontWeight="Bold" Margin="0,10,0,0" Grid.Row="2"/>
            <TextBlock x:Name="StatusText" Margin="120,0,0,0" Grid.Row="2"/>
            <TextBlock Text="Assignee:" FontWeight="Bold" Margin="0,10,0,0" Grid.Row="3"/>
            <TextBlock x:Name="AssigneeText" Margin="120,0,0,0" Grid.Row="3"/>
            <TextBlock Text="Web Link:" FontWeight="Bold" Margin="0,10,0,0" Grid.Row="4"/>
            <TextBlock Grid.Row="4" Margin="120,10,0,0">
                <Hyperlink x:Name="IssueWebLink" Click="IssueWebLink_Click">
                    <Run Text="View in Jira"/>
                </Hyperlink>
            </TextBlock>
            <TextBlock Text="Description:" FontWeight="Bold" Margin="0,10,0,0" Grid.Row="5"/>
            <ScrollViewer Grid.Row="6" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="DescriptionText" TextWrapping="Wrap"/>
            </ScrollViewer>
            <GroupBox Header="Comments" Grid.Row="7" Margin="0,10,0,0" Padding="5">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Height="180">
                    <ItemsControl x:Name="CommentsList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Padding="4" Margin="0,0,0,4">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Author}" FontWeight="Bold"/>
                                            <TextBlock Text=" - " />
                                            <TextBlock Text="{Binding Created, StringFormat=yyyy-MM-dd HH:mm}" Foreground="Gray"/>
                                            <TextBlock Text=" (Updated: " Foreground="Gray" Visibility="{Binding Updated, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                            <TextBlock Text="{Binding Updated, StringFormat=yyyy-MM-dd HH:mm}" Foreground="Gray" Visibility="{Binding Updated, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                            <TextBlock Text=")" Foreground="Gray" Visibility="{Binding Updated, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding Body}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </GroupBox>
        </Grid>
        <!-- Time Tracking Details -->
        <Border Grid.Column="1" Background="#AA222222" CornerRadius="8" Padding="10" HorizontalAlignment="Left" VerticalAlignment="Top">
            <StackPanel>
                <Button x:Name="StartTrackingButton" Content="Start Time Tracking" Width="180" Height="32" Margin="0,0,10,0" Click="StartTrackingButton_Click"/>
                <Button x:Name="StopTrackingButton" Content="Stop Time Tracking" Width="180" Height="32" Margin="0,10,10,0" Click="StopTrackingButton_Click" Visibility="Collapsed"/>
                <TextBlock x:Name="TotalTimeText" FontWeight="Bold" Margin="0,10,0,5"/>
                <TextBlock Text="Time Tracking Records:" FontWeight="Bold" Margin="0,10,0,5"/>
                <DataGrid x:Name="TimeTrackingGrid" MinHeight="120" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Margin="0,0,0,0" VerticalAlignment="Top">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Started" Binding="{Binding Started}" Width="200"/>
                        <DataGridTextColumn Header="Duration" Binding="{Binding Duration}" Width="200"/>
                        <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="80"/>
                        <DataGridTemplateColumn Header="Edit" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Edit" Click="EditRecord_Click" Visibility="{Binding CanEdit, Converter={StaticResource BoolToVisibilityConverter}}" Tag="{Binding RecordId}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Delete" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Delete" Click="DeleteRecord_Click" Visibility="{Binding CanEdit, Converter={StaticResource BoolToVisibilityConverter}}" Tag="{Binding RecordId}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
